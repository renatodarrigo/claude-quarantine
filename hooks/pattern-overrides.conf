# claude-guard — Pattern Severity Overrides
# Format: PATTERN_REGEX=NEW_SEVERITY
# Matches against the pattern text (not the content), case-insensitive.
# Allows overriding built-in pattern severities without editing injection-patterns.conf.
#
# Example: Downgrade all social_engineering patterns to LOW:
# social_engineering=LOW
#
# Example: Upgrade a specific pattern to HIGH:
# fake urgency=HIGH
